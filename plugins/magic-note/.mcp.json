{
  "mcpServers": {
    "magic-note": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/src/mcp/server.ts"],
      "env": {
        "MAGIC_NOTE_HOME": "~/.magic-note"
      }
    }
  }
}
