# Magic Note 사용자 가이드

Magic Note는 AI 코딩 워크플로우를 위한 노트 및 워크플로우 관리 플러그인입니다.

## 목차

1. [빠른 시작](#빠른-시작)
2. [노트 관리](#노트-관리)
3. [워크플로우 관리](#워크플로우-관리)
4. [자동화 기능](#자동화-기능)
5. [고급 사용법](#고급-사용법)
6. [문제 해결](#문제-해결)

---

## 빠른 시작

### 설치 확인

```bash
# 플러그인 설치 확인
/plugin list

# Magic Note 명령어 확인
/magic-note:list
```

### 첫 번째 노트 만들기

```bash
# 새 노트 생성
/magic-note:add 프로젝트 시작 계획

# 또는 대화 내용 저장
/magic-note:save the implementation plan
```

### 저장 위치

모든 데이터는 현재 프로젝트 디렉토리의 `.magic-note/` 폴더에 저장됩니다:

```
your-project/
└── .magic-note/
    ├── projects/    # 노트 파일
    ├── workflows/   # 워크플로우 데이터
    └── ...
```

---

## 노트 관리

### 노트 타입

| 타입 | 용도 | 언제 사용? |
|------|------|-----------|
| `prompt` | 재사용 프롬프트 | 코드 리뷰 체크리스트, 반복 작업 |
| `plan` | 구현 계획 | 기능 개발 로드맵, 마일스톤 |
| `choice` | 기술 결정 | 프레임워크 선택 이유, 아키텍처 결정 |
| `insight` | 학습 내용 | 세션 중 배운 팁, 패턴 |

### 주요 명령어

#### 노트 생성

```bash
# 기본 노트 생성
/magic-note:add API 인증 구현 계획

# 타입 지정
/magic-note:add type:plan JWT 인증 설계

# 태그 포함
/magic-note:add #auth #security 보안 검토 체크리스트
```

#### 노트 조회

```bash
# 전체 목록
/magic-note:list

# 태그로 필터
/magic-note:list #auth

# 타입으로 필터
/magic-note:list type:plan

# 검색
/magic-note:search jwt authentication
```

#### 노트 활용

```bash
# 노트 내용 보기
/magic-note:view abc123

# 노트 로드 (컨텍스트에 추가)
/magic-note:load abc123

# 노트 복사 (클립보드)
/magic-note:copy abc123
```

#### 노트 편집

```bash
# 태그 추가
/magic-note:edit abc123 add tag:done

# 태그 제거
/magic-note:edit abc123 remove tag:wip

# 노트 삭제
/magic-note:delete abc123
```

### 대화 저장

세션 중 중요한 내용을 바로 저장:

```bash
# 계획 저장
/magic-note:save the authentication plan

# 결정 저장
/magic-note:save the framework decision

# 프롬프트 저장
/magic-note:save the code review checklist
```

---

## 워크플로우 관리

워크플로우는 복잡한 구현 계획을 체계적으로 관리합니다.

### 워크플로우 구조

```
Workflow (워크플로우)
├── Task 1 (태스크)
│   ├── Step 1.1 (스텝)
│   ├── Step 1.2
│   └── Step 1.3
├── Task 2
│   └── ...
└── Checkpoints (체크포인트)
```

### 워크플로우 생성

Claude에게 구현 계획을 요청하면 자동으로 워크플로우 변환을 제안합니다:

```
사용자: "사용자 인증 시스템 구현해줘"

Claude: [계획 생성 후]
📋 이 계획을 워크플로우로 변환할까요?
- 5개 태스크, 예상 완료율 추적
- 체크포인트로 진행 상황 저장 가능
```

### 워크플로우 사용

#### 상태 확인

```
사용자: "현재 진행 상황 보여줘"

Claude: 📊 Workflow Status: 사용자 인증 시스템

Progress: ████████░░░░░░░░ 53%

✅ Completed (3):
- 사용자 모델 설계
- JWT 토큰 생성
- 로그인 API

🔄 In Progress (1):
- 리프레시 토큰 구현

⬜ Remaining (2):
- 로그아웃 처리
- 세션 관리
```

#### 작업 재개

```
사용자: "어제 하던 작업 이어서 해줘"

Claude: 📂 Found active workflow: 사용자 인증 시스템
Last checkpoint: 2026-01-03 (Task 3 완료)

Resuming from: 리프레시 토큰 구현
```

#### 체크포인트

중요한 마일스톤에서 진행 상황을 저장:

```
사용자: "여기까지 저장해줘"

Claude: 💾 Checkpoint created!
- ID: cp_abc123
- Progress: 53% (3/5 tasks)
- Note: 인증 기본 기능 완료
```

### 태스크 관리

#### 태스크 상태

| 상태 | 설명 |
|------|------|
| `pending` | 대기 중 |
| `in_progress` | 진행 중 |
| `completed` | 완료 |
| `failed` | 실패 |
| `skipped` | 건너뜀 |
| `blocked` | 차단됨 |

#### 의존성 관리

태스크 간 의존성을 설정하면 순서대로 실행:

```
Task 1: 데이터베이스 스키마 설계
Task 2: API 엔드포인트 구현 (depends on: Task 1)
Task 3: 프론트엔드 연동 (depends on: Task 2)
```

---

## 자동화 기능

### Skills (자동 호출)

Claude가 상황에 맞게 자동으로 호출하는 기능들:

#### 노트 스킬

| 스킬 | 자동 호출 시점 |
|------|--------------|
| `auto-save-plan` | 구현 계획 생성 시 |
| `prompt-library` | 코드 리뷰/리팩토링 요청 시 |
| `decision-logger` | 기술 선택 결정 시 |
| `auto-capture-insight` | ★ Insight 블록 생성 시 |

#### 워크플로우 스킬

| 스킬 | 자동 호출 시점 |
|------|--------------|
| `workflow` | 복잡한 계획 생성 시 |
| `resume` | 작업 재개 요청 시 |
| `status` | 진행 상황 확인 요청 시 |
| `checkpoint` | 마일스톤 완료 시 |

### Hooks (이벤트 자동화)

#### SessionStart

세션 시작 시 관련 노트 자동 검색:

```
[세션 시작]
📚 Magic Note: Found 3 related notes:
- Auth Implementation Plan (plan)
- API Review Checklist (prompt)

Use `/magic-note:load [id]` to load any note.
```

#### SessionEnd

세션 종료 전 저장 제안:

```
[세션 종료 전]
💾 Would you like to save this session's content?
- Implementation plan for user authentication

Quick save: `/magic-note:save`
```

### 인사이트 자동 캡처

Learning/Explanatory 모드에서 생성된 인사이트가 자동 저장됩니다:

```
★ Insight ─────────────────────────────────────
React useEffect 사용 시 주의사항:
- dependency array를 정확히 지정해야 무한 루프 방지
- cleanup 함수로 메모리 누수 방지
─────────────────────────────────────────────────

[자동 저장됨]
💡 Insight added! Project: my-app (Total: 5)
```

인사이트 확인:
```bash
/magic-note:insights my-app
```

---

## 고급 사용법

### 프로젝트별 관리

각 프로젝트는 독립적인 `.magic-note` 디렉토리를 가집니다:

```bash
# 프로젝트 A
cd /path/to/project-a
# → .magic-note/에 project-a의 노트/워크플로우 저장

# 프로젝트 B
cd /path/to/project-b
# → .magic-note/에 project-b의 노트/워크플로우 저장
```

### 버전 관리

`.magic-note`를 git에 포함하거나 제외할 수 있습니다:

```bash
# git에 포함 (팀 공유)
git add .magic-note/

# git에서 제외 (개인용)
echo ".magic-note/" >> .gitignore
```

### 커스텀 저장 위치

환경변수로 저장 위치 변경:

```bash
# 전역 저장소 사용
export MAGIC_NOTE_STORAGE=~/.magic-note-global

# 또는 특정 경로
export MAGIC_NOTE_STORAGE=/shared/team-notes
```

### 노트-워크플로우 연결

노트를 워크플로우나 태스크에 연결:

```
# Claude와 대화로 연결
"이 계획 노트를 현재 워크플로우에 연결해줘"

# 연결 확인
워크플로우 상태를 보면 관련 노트가 표시됨
```

### 배치 실행

여러 태스크를 동시에 처리:

```
사용자: "다음 태스크들 한번에 시작해"

Claude: 🚀 Starting batch execution
Batch #1: 3 tasks
- Task A: API endpoint
- Task B: Database migration
- Task C: Unit tests
```

---

## 문제 해결

### 자주 묻는 질문

#### Q: 노트가 보이지 않아요

```bash
# 현재 디렉토리 확인
pwd

# .magic-note 폴더 확인
ls -la .magic-note/

# 노트 인덱스 확인
cat .magic-note/index.json
```

#### Q: 이전 프로젝트 데이터는 어디에?

v1.1.0 이전에는 `~/.magic-note`에 저장되었습니다:

```bash
# 기존 데이터 마이그레이션
cp -r ~/.magic-note /path/to/project/.magic-note
```

#### Q: 워크플로우가 복원되지 않아요

```bash
# 체크포인트 목록 확인
# Claude에게 "워크플로우 체크포인트 목록 보여줘" 요청

# 또는 직접 확인
ls .magic-note/workflows/*/checkpoints/
```

### 데이터 백업

```bash
# 전체 백업
cp -r .magic-note .magic-note-backup-$(date +%Y%m%d)

# 특정 워크플로우 백업
cp -r .magic-note/workflows/wf_xxx ./workflow-backup/
```

### 초기화

```bash
# 전체 초기화 (주의: 모든 데이터 삭제)
rm -rf .magic-note

# 다음 사용 시 자동으로 새로 생성됨
```

---

## 팁과 트릭

### 효율적인 노트 관리

1. **태그 일관성**: 태그 명명 규칙을 정해서 사용
   - `#auth`, `#api`, `#frontend` 등

2. **타입 활용**: 노트 타입을 적절히 사용
   - 계획은 `plan`, 프롬프트는 `prompt`

3. **정기 정리**: `/magic-note:list`로 주기적으로 확인하고 불필요한 노트 정리

### 워크플로우 베스트 프랙티스

1. **적절한 태스크 크기**: 1-2시간 내 완료 가능한 크기로 분할
2. **체크포인트 활용**: 중요 마일스톤마다 체크포인트 생성
3. **의존성 설정**: 순서가 중요한 태스크는 의존성 설정

### 팀 협업

1. `.magic-note`를 git에 포함하면 계획/결정 공유 가능
2. 코드 리뷰 체크리스트를 `prompt` 타입으로 저장하여 팀 공유
3. 기술 결정은 `choice` 타입으로 기록하여 히스토리 보존

---

## 버전 정보

- **현재 버전**: 1.1.0
- **최소 Claude Code 버전**: v1.0.33
- **지원 런타임**: Bun (권장), Node.js 22.18+

자세한 변경 사항은 [CHANGELOG.md](../CHANGELOG.md)를 참조하세요.
