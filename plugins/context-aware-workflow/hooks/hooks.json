{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "CAW (Context-Aware Workflow) plugin is active. Available commands: /cw:status (check workflow state), /cw:start (begin new task), /cw:init (initialize environment)."
          }
        ]
      }
    ]
  }
}
