---
name: feedback-loop
description: "This skill should be used when the user wants to collect, analyze, or report skill execution feedback. Detects repeated failures, user corrections, and performance anomalies. Triggers: 'feedback start', 'feedback analyze', 'feedback report', '스킬 피드백 분석', 'analyze skill performance'."
---

# Feedback Loop - 스킬 개선 피드백 시스템

스킬 실행 결과를 체계적으로 수집하고 분석하여 자동으로 개선 제안을 생성합니다.

## 실행 프로세스 개요

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    FEEDBACK LOOP PROCESS                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  0. INTENT CLARIFICATION                                                │
│     └─ 대상 스킬 및 분석 범위 확인                                      │
│                                                                         │
│  1. EVENT COLLECTION                                                    │
│     └─ 스킬 실행 이벤트 수집 (start/complete/failure/correction)        │
│                                                                         │
│  2. PATTERN ANALYSIS                                                    │
│     └─ 반복 실패, 반복 수정, 성능 이상 패턴 감지                        │
│                                                                         │
│  3. REPORT GENERATION                                                   │
│     └─ 분석 리포트 및 개선 제안 생성                                    │
│                                                                         │
│  4. APPLICATION (NEW)                                                   │
│     └─ 스킬 존재 확인 → 적용 가이드 생성 → 스킬 개선                   │
│                                                                         │
│  5. VERIFICATION                                                        │
│     └─ 시스템 정상 동작 검증                                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Phase 0: 의도 명확화 (Intent Clarification)

> **전략**: `minimal` - 명령어에서 대부분 정보 추론 가능

### 스킵 조건

다음 조건에서는 Phase 1로 바로 진행:
- `feedback start <skill>` 형태로 스킬 이름이 명시된 경우
- `feedback analyze <skill>` 형태로 분석 대상이 명시된 경우
- `feedback report <skill>` 형태로 리포트 대상이 명시된 경우

### Required Questions

| ID | 질문 | 타입 | 기본값 |
|----|------|------|--------|
| Q1 | 어떤 스킬의 피드백을 다루시겠습니까? | text | - |
| Q2 | 어떤 작업을 하시겠습니까? (수집/분석/리포트) | choice | - |

### Conditional Questions

| ID | 조건 | 질문 |
|----|------|------|
| CQ1 | 작업 = 분석 | 분석 기간은 며칠로 하시겠습니까? (기본: 7일) |
| CQ2 | 작업 = 리포트 | 출력 형식은 무엇으로 하시겠습니까? (md/json) |

---

## Phase 1: 이벤트 수집 (Event Collection)

### 목적
스킬 실행 과정에서 발생하는 주요 이벤트를 JSONL 형식으로 저장

### 수집 이벤트 유형

| 이벤트 | 설명 | 트리거 |
|--------|------|--------|
| `execution_start` | 스킬 실행 시작 | 스킬 실행 시 |
| `execution_complete` | 스킬 실행 완료 | 검증 완료 후 |
| `verification_failure` | 검증 실패 | 검증 항목 실패 시 |
| `user_correction` | 사용자 수정 | 출력 수정 시 |

### 실행 명령어

```bash
# 1. 스킬 실행 시작 기록 (session_id 반환)
session=$(feedback start <skill_name> [version] [input_summary])
# 예: session=$(feedback start vue-project-analyzer 2.0.0 "/path/to/project")

# 2. 검증 실패 기록
feedback failure <skill> <session> <check_id> <check_name> <priority> [error_message]
# 예: feedback failure vue-project-analyzer "$session" STRUCT-001 "버전명시" must "Not found"

# 3. 사용자 수정 기록
feedback correction <skill> <session> <file_path> <action> [section] [+lines] [-lines]
# 예: feedback correction vue-project-analyzer "$session" ARCHITECTURE.md modify overview 5 2

# 4. 스킬 실행 완료 기록
feedback complete <skill> <session> <duration> <total> <pass> <fail> <warn>
# 예: feedback complete vue-project-analyzer "$session" 45 10 9 1 0
```

### 데이터 저장 위치

```
~/.claude/feedback/
└── logs/
    └── {skill_name}/
        └── {YYYY-MM-DD}.jsonl     # 일별 이벤트 로그
```

---

## Phase 2: 패턴 분석 (Pattern Analysis)

### 목적
수집된 이벤트에서 반복 패턴을 감지하여 개선 포인트 식별

### 분석 패턴

| 패턴 | 트리거 조건 | 우선순위 | 제안 조치 |
|------|------------|----------|----------|
| 🔴 반복 실패 | 동일 check_id 3회+ 실패 | HIGH | 검증 로직/가이드 수정 |
| 🟡 반복 수정 | 동일 섹션 3회+ 수정 | MEDIUM | 생성 가이드 개선 |
| 🟢 성능 이상 | 실행 시간 > 평균 + 2σ | LOW | 최적화 가이드 추가 |

### 실행 명령어

```bash
# 패턴 분석 실행
feedback analyze <skill> [--period=7]

# 예시
feedback analyze vue-project-analyzer --period=14
```

### 분석 알고리즘

```python
# 반복 실패 감지
failures = group_by(events, 'check_id')
repeated = filter(failures, count >= 3)

# 반복 수정 감지
corrections = group_by(events, 'section')
repeated = filter(corrections, count >= 3)

# 성능 이상 감지
durations = [e.duration for e in complete_events]
threshold = mean(durations) + 2 * std(durations)
anomalies = filter(durations, d > threshold)
```

---

## Phase 3: 리포트 생성 (Report Generation)

### 목적
분석 결과를 사용자가 이해하기 쉬운 형태로 출력

### 실행 명령어

```bash
# 리포트 생성
feedback report <skill> [--format=md|json]

# 예시
feedback report vue-project-analyzer --format=md
```

### 출력 형식

#### Markdown 리포트 예시

```markdown
# vue-project-analyzer 피드백 분석 리포트

**분석 기간**: 2025-01-12 ~ 2025-01-19
**총 실행**: 15회

## 반복 실패 패턴

| Check ID | 이름 | 실패 횟수 | 우선순위 |
|----------|------|----------|----------|
| STRUCT-001 | 버전 명시 | 5회 | HIGH |

## 반복 수정 패턴

| 파일 | 섹션 | 수정 횟수 |
|------|------|----------|
| ARCHITECTURE.md | overview | 4회 |
| ARCHITECTURE.md | components | 3회 |

## 성능 분석

- 평균 실행 시간: 42.3초
- 표준 편차: 8.7초
- 이상 감지: 2건

## 개선 제안

1. 🔴 **[HIGH]** STRUCT-001 검증 로직 또는 가이드 수정
2. 🟡 **[MEDIUM]** overview 섹션 생성 가이드 개선
```

### 출력 저장 위치

```
~/.claude/feedback/
├── reports/
│   └── {skill_name}/
│       └── report-{date}.md       # 분석 리포트
└── suggestions/
    └── {skill_name}/
        └── {suggestion_id}.md     # 개선 제안
```

---

## Phase 4: 적용 (Application)

### 목적
분석 결과를 실제 스킬에 적용하기 위한 가이드 생성

### 스킬 존재 확인

```bash
# 스킬 존재 여부 확인
feedback verify-skill <skill>

# 예시
feedback verify-skill dotnet-project-analyzer
```

**출력 예시:**
```json
{
  "exists": false,
  "skill_name": "dotnet-project-analyzer",
  "path": null,
  "type": null,
  "version": null,
  "message": "스킬 'dotnet-project-analyzer'이(가) index.yaml에 등록되어 있지 않습니다."
}
```

### 적용 가이드 생성

```bash
# 적용 가이드 생성 (dry-run)
feedback apply <skill> --dry-run

# 예시
feedback apply vue-project-analyzer --dry-run
```

### 스킬 미존재 시 처리 워크플로우

```
feedback analyze <skill>
    ↓
스킬 존재 확인
    ├─ 존재함 (exists: true)
    │   └─ 분석 + 적용 가이드 생성
    │       ├─ HIGH 우선순위: verification/checklist.yaml 수정
    │       └─ MEDIUM 우선순위: SKILL.md 가이드 보강
    │
    └─ 존재하지 않음 (exists: false)
        └─ 안내 메시지 출력
            ├─ 옵션 1: intent-skill-creator로 새 스킬 생성
            ├─ 옵션 2: 피드백 데이터 정리 (삭제)
            └─ 옵션 3: 기존 스킬 이름으로 데이터 이전
```

### 적용 가이드 구조

| 필드 | 설명 |
|------|------|
| `skill_exists` | 스킬 존재 여부 |
| `applicable` | 제안 적용 가능 여부 |
| `total_suggestions` | 총 제안 수 |
| `actions` | 구체적 적용 액션 목록 |
| `message` | 상태 메시지 |

### 적용 예시 (스킬 존재 시)

```bash
# 1. 적용 가이드 확인
feedback apply vue-project-analyzer --dry-run

# 2. 출력된 가이드에 따라 파일 수정
# - verification/checklist.yaml의 반복 실패 항목 수정
# - SKILL.md의 관련 섹션 가이드 보강

# 3. 재검증
feedback analyze vue-project-analyzer --period=7
```

---

## Phase 5: 검증 (Verification)

### 자동 검증 실행

```bash
bash verification/run-verification.sh
```

### 검증 체크리스트

#### MUST (필수)
- [ ] 피드백 디렉토리 쓰기 가능
- [ ] Python3 사용 가능
- [ ] 모든 이벤트 타입 정상 기록
- [ ] JSONL 형식 유효
- [ ] 분석기 정상 실행
- [ ] 리포트 생성 성공

#### SHOULD (권장)
- [ ] 이벤트 스키마 준수
- [ ] 리포트 필수 섹션 포함
- [ ] 성능 분석 데이터 존재

#### COULD (선택)
- [ ] 로그 보존 정책 적용
- [ ] 테스트 데이터 자동 정리

---

## 오류 복구

| 상황 | 원인 | 조치 |
|------|------|------|
| session_id 생성 실패 | UUID 생성 불가 | Python fallback 사용 또는 date 기반 생성 |
| 로그 디렉토리 쓰기 실패 | 권한 문제 | `chmod 755 ~/.claude/feedback` |
| 분석기 실행 실패 | Python3 없음 | Python3 설치 또는 경로 확인 |
| JSON 파싱 에러 | 손상된 로그 | 해당 줄 건너뛰기 또는 로그 복구 |

---

## 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    FEEDBACK LOOP SYSTEM                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐            │
│  │  Skill       │────▶│  Collector   │────▶│  Logs        │            │
│  │  Execution   │     │  (Bash)      │     │  (JSONL)     │            │
│  └──────────────┘     └──────────────┘     └──────────────┘            │
│                                                   │                     │
│                                                   ▼                     │
│                       ┌──────────────┐     ┌──────────────┐            │
│                       │  Analyzer    │◀────│  Pattern     │            │
│                       │  (Python)    │     │  Detection   │            │
│                       └──────────────┘     └──────────────┘            │
│                              │                                          │
│                              ▼                                          │
│  ┌──────────────┐     ┌──────────────┐                                 │
│  │  Skill       │◀────│  Improvement │                                 │
│  │  Update      │     │  Suggestions │                                 │
│  └──────────────┘     └──────────────┘                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Quick Reference

| 명령 | 용도 |
|------|------|
| `feedback start <skill>` | 실행 시작, session_id 반환 |
| `feedback complete <skill> <session> ...` | 실행 완료 기록 |
| `feedback failure <skill> <session> ...` | 검증 실패 기록 |
| `feedback correction <skill> <session> ...` | 사용자 수정 기록 |
| `feedback analyze <skill>` | 패턴 분석 |
| `feedback report <skill>` | 리포트 생성 |
| `feedback verify-skill <skill>` | 스킬 존재 확인 |
| `feedback apply <skill> [--dry-run]` | 적용 가이드 생성 |

---

## 스킬 통합 예시

다른 스킬에서 feedback-loop를 통합하는 방법:

```bash
#!/bin/bash
# 스킬 실행 래퍼 예시

SKILL_NAME="my-skill"

# 1. 실행 시작 기록
SESSION=$(feedback start "$SKILL_NAME" "1.0.0" "$INPUT")

# 2. 스킬 실행
run_skill "$INPUT"

# 3. 검증 실행 및 실패 기록
while read -r check_id check_name priority result; do
    if [ "$result" = "FAIL" ]; then
        feedback failure "$SKILL_NAME" "$SESSION" "$check_id" "$check_name" "$priority"
    fi
done < <(run_verification)

# 4. 실행 완료 기록
feedback complete "$SKILL_NAME" "$SESSION" "$DURATION" "$TOTAL" "$PASS" "$FAIL" "$WARN"
```
